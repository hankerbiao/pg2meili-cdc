# 纯接口文档（汇总版）

提供的接口均为测试接口，生产环境需要前端先测速，找到最优的服务调用，本文档仅供开发参考

目前已存储数据，集合：testcases，可使用以下 token 进行搜索测试

【搜索只读 Token】
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJhcHBfbmFtZSI6ImxpYmlhbyIsInNjb3BlcyI6WyJzZWFyY2g6cmVhZCJdLCJleHAiOjE4MDE1MjYzOTcsImlhdCI6MTc3MDc4ODk3NiwianRpIjoiMTBhNDhjYmEtMmRkZC00M2Y4LTg4OTgtOGY3ZWFlYzI3MDljIn0.Bf2E51dWPIbgsJlAjkBvzZV7pn2oKUJjg70r8Holr1M

## 服务地址与 Token 申请

- **数据管理服务**：`http://10.32.129.188:8080`
- **搜索服务**：`http://10.32.129.188:8091`
- **Token 申请地址**：`http://10.32.129.188:8080/app/register`

Token 申请流程（简要）：
1. 打开注册页面填写 `app_name`、失效日期、接收人 ITCode。
2. 提交后等待管理员审核。
3. 审核通过后通过内部渠道下发 Token。

---

## 1. 搜索接口（Search API）

### 1.1 接口概览

- **接口地址**：`POST /search?collection={collection}`
- **基础域名**：`http://10.32.129.188:8091`
- **协议**：HTTPS/HTTP
- **鉴权**：`Authorization: Bearer <JWT>`
- **数据格式**：`Content-Type: application/json`

### 1.2 公共请求头

- `Authorization`：必填，`Bearer <JWT>`
- `Content-Type`：必填，`application/json`

### 1.3 请求体参数（Meilisearch 1.34.3）

请求体透传到 Meilisearch `POST /indexes/{index}/search`。服务端默认会补 `showRankingScore: true`（如未显式提供）。

#### 常用参数

| 参数 | 类型 | 必填 | 说明 | 示例 |
| :--- | :--- | :--- | :--- | :--- |
| `q` | string | 否 | 查询关键字，空字符串表示全量检索 | `"电源"` |
| `offset` | integer | 否 | 跳过文档数量（与 `limit` 配合分页） | `0` |
| `limit` | integer | 否 | 返回条数 | `20` |
| `page` | integer | 否 | 指定页码（与 `hitsPerPage` 配合分页） | `1` |
| `hitsPerPage` | integer | 否 | 每页条数 | `20` |
| `filter` | string/array | 否 | 过滤条件表达式 | `tags = "BMC"` |
| `sort` | array | 否 | 排序规则 | `["created_at:desc"]` |
| `attributesToRetrieve` | array | 否 | 返回字段白名单 | `["id", "title", "author"]` |
| `attributesToHighlight` | array | 否 | 高亮字段列表 | `["*"]` |
| `attributesToCrop` | array | 否 | 裁剪字段列表 | `["content"]` |
| `cropLength` | integer | 否 | 裁剪长度（词数） | `60` |

#### 高级参数

| 参数 | 类型 | 必填 | 说明 | 示例 |
| :--- | :--- | :--- | :--- | :--- |
| `facets` | array | 否 | 返回聚合维度 | `["tags"]` |
| `distinct` | string | 否 | 去重字段 | `"id"` |
| `highlightPreTag` | string | 否 | 高亮前置标记 | `"<mark>"` |
| `highlightPostTag` | string | 否 | 高亮后置标记 | `"</mark>"` |
| `cropMarker` | string | 否 | 裁剪省略标记 | `"…"` |
| `attributesToSearchOn` | array | 否 | 仅在指定字段内搜索 | `["title", "content"]` |
| `showMatchesPosition` | boolean | 否 | 返回匹配位置 | `true` |
| `matchingStrategy` | string | 否 | 匹配策略 | `"last"` |
| `showRankingScore` | boolean | 否 | 返回排名分数 | `true` |
| `showRankingScoreDetails` | boolean | 否 | 返回详细排名分数 | `true` |
| `rankingScoreThreshold` | number | 否 | 过滤低分结果 | `0.2` |

> 注意：`offset/limit` 与 `page/hitsPerPage` 只需任选一组。

### 1.4 查询参数

| 参数 | 位置 | 类型 | 必填 | 说明 | 示例 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `collection` | query | string | 是 | 集合名称，用于定位索引 | `testcases` |

### 1.5 响应结果

接口返回 Meilisearch 原始搜索结果，`hits` 内字段来自业务文档本身。常见字段示例：

- `hits`：文档数组
- `hits[i].id`：内部 ID
- `hits[i].ext_id`：外部编号
- `hits[i].name`：名称
- `hits[i].summary`：摘要
- `hits[i].tags`：标签数组
- `hits[i]._formatted`：高亮字段

### 1.7 示例（返回字段裁剪）

```bash
curl -X POST "http://10.32.129.188:8091/search?collection=testcases" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJhcHBfbmFtZSI6ImxpYmlhbyIsInNjb3BlcyI6WyJzZWFyY2g6cmVhZCJdLCJleHAiOjE4MDE1MjYzOTcsImlhdCI6MTc3MDc4ODk3NiwianRpIjoiMTBhNDhjYmEtMmRkZC00M2Y4LTg4OTgtOGY3ZWFlYzI3MDljIn0.Bf2E51dWPIbgsJlAjkBvzZV7pn2oKUJjg70r8Holr1M" \
  --data-raw '{
    "q": "电源",
    "attributesToRetrieve": ["id", "name", "summary", "tags"],
    "attributesToCrop": ["summary"],
    "cropLength": 60
  }'
```

---

## 2. 代理节点在线列表

- **接口地址**：`GET /api/v1/agents/online`
- **基础地址**：`http://10.32.129.188:8080`
- **鉴权**：`Authorization: Bearer <YOUR_JWT>`

**请求示例（curl）**：

```bash
curl -X GET "http://10.32.129.188:8080/api/v1/agents/online" \
  -H "Authorization: Bearer <YOUR_JWT>"
```

**返回示例**：

```json
[
  { "ip": "10.17.154.252", "port": 8091, "hostname": "edge-tj-01" },
  { "ip": "10.32.129.188", "port": 8091, "hostname": "edge-bj-01" }
]
```

---

## 3. 数据管理接口（Management API）

### 3.1 基本信息

- **基础地址**（示例）：`http://10.32.129.188:8080`
- **统一前缀**：`/api/v1`
- **鉴权**：`Authorization: Bearer <token>`（管理/写入仅接受后端读写 Token）
- **返回格式**：统一为 `{ data, message }`

### 3.2 索引管理

#### 3.2.1 获取索引列表

- **接口地址**：`GET /api/v1/indexes`

**请求示例（curl）**：

```bash
curl -X GET "http://10.32.129.188:8080/api/v1/indexes?limit=100&offset=0" \
  -H "Authorization: Bearer <YOUR_JWT>"
```

**返回示例**：

```json
{
  "data": ["testcases", "requirements", "bugs"],
  "message": "ok"
}
```

#### 3.2.2 删除索引

- **接口地址**：`DELETE /api/v1/indexes/{collection}`

**请求示例（curl）**：

```bash
curl -X DELETE "http://10.32.129.188:8080/api/v1/indexes/requirements" \
  -H "Authorization: Bearer <YOUR_JWT>"
```

**返回示例**：

```json
{
  "data": {
    "status": "success",
    "collection": "requirements",
    "deleted_count": 128
  },
  "message": "ok"
}
```

#### 3.2.3 设置索引字段

- **接口地址**：`POST /api/v1/indexes/{collection}/settings`

**请求示例（curl）**：

```bash
curl -X POST "http://10.32.129.188:8080/api/v1/indexes/testcases/settings" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <YOUR_JWT>" \
  -d '{
    "filterableAttributes": ["author"],
    "sortableAttributes": ["author"]
  }'
```

**返回示例**：

```json
{
  "data": {
    "status": "success",
    "collection": "testcases",
    "index_uid": "libiao_testcases"
  },
  "message": "ok"
}
```

### 3.3 通用文档管理

#### 3.3.1 创建 / 更新文档

- **接口地址**：`POST /api/v1/data/{collection}`

**请求示例（curl）**：

```bash
curl -X POST "http://10.32.129.188:8080/api/v1/data/testcases" \
  -H "Content-Type: application/json" \
  -d '{
    "id": "tc_001",
    "title": "登录功能测试",
    "priority": "high",
    "status": "active"
  }'
```

**返回示例**：

```json
{
  "data": {
    "status": "success",
    "id": "tc_001",
    "collection": "testcases"
  },
  "message": "ok"
}
```

#### 3.3.2 获取文档详情

- **接口地址**：`GET /api/v1/data/{collection}/{id}`

**请求示例（curl）**：

```bash
curl -X GET "http://10.32.129.188:8080/api/v1/data/testcases/tc_001" \
  -H "Authorization: Bearer <YOUR_JWT>"
```

**返回示例**：

```json
{
  "data": {
    "id": "tc_001",
    "title": "登录功能测试",
    "priority": "high",
    "status": "active",
    "collection": "testcases",
    "app_name": "my-app"
  },
  "message": "ok"
}
```

#### 3.3.3 批量创建 / 更新

- **接口地址**：`POST /api/v1/data/{collection}/batch`

**请求示例（curl）**：

```bash
curl -X POST "http://10.32.129.188:8080/api/v1/data/testcases/batch" \
  -H "Content-Type: application/json" \
  -d '{
    "items": [
      { "id": "tc_001", "title": "登录功能测试", "priority": "high" },
      { "id": "tc_002", "title": "注册功能测试", "priority": "medium" }
    ]
  }'
```

**返回示例**：

```json
{
  "data": {
    "status": "success",
    "collection": "testcases",
    "count": 2,
    "ids": ["tc_001", "tc_002"]
  },
  "message": "ok"
}
```

#### 3.3.4 删除文档

- **接口地址**：`DELETE /api/v1/data/{collection}/{id}`

**请求示例（curl）**：

```bash
curl -X DELETE "http://10.32.129.188:8080/api/v1/data/testcases/tc_001" \
  -H "Authorization: Bearer <YOUR_JWT>"
```

**返回示例**：

```json
{
  "data": {
    "status": "success",
    "id": "tc_001",
    "collection": "testcases"
  },
  "message": "ok"
}
```

#### 3.3.5 列表查询

- **接口地址**：`GET /api/v1/data/{collection}`

**请求示例（curl）**：

```bash
curl -X GET "http://10.32.129.188:8080/api/v1/data/testcases?limit=20&offset=0" \
  -H "Authorization: Bearer <YOUR_JWT>"
```

**返回示例**：

```json
{
  "data": [
    {
      "id": "tc_001",
      "title": "登录功能测试",
      "priority": "high",
      "status": "active",
      "collection": "testcases",
      "app_name": "my-app"
    }
  ],
  "message": "ok"
}
```

---

## 4. Token 撤销与广播

### 4.1 撤销 token（按 jti）

- **接口地址**：`POST /api/v1/auth/tokens/revoke`
- **权限**：仅允许 `admin:token:revoke` scope

**请求示例**：

```bash
curl -X POST "http://10.32.129.188:8080/api/v1/auth/tokens/revoke" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <YOUR_JWT>" \
  -d '{
    "jti": "b7c9f0e3-1a2b-4c4e-9d8a-0f7d7c4c0a12",
    "reason": "app 停用"
  }'
```

### 4.2 查询 token 是否已撤销

- **接口地址**：`GET /api/v1/auth/tokens/revoked/{jti}`

**请求示例**：

```bash
curl -X GET "http://10.32.129.188:8080/api/v1/auth/tokens/revoked/b7c9f0e3-1a2b-4c4e-9d8a-0f7d7c4c0a12"
```

### 4.3 Kafka 撤销广播事件

- **Topic**：`token.revocations`
- **消息体示例**：

```json
{
  "version": 1,
  "event": "token_revoked",
  "app_name": "demo-app",
  "jti": "b7c9f0e3-1a2b-4c4e-9d8a-0f7d7c4c0a12",
  "reason": "app 停用",
  "ts": 1732000000
}
```

---

## 5. 错误返回格式

```json
{
  "data": null,
  "message": "错误信息"
}
```
