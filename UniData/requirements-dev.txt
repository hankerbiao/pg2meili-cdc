# UniData 测试依赖（本地 / CI 安装用，版本已锁定以保证可复现）
# 安装：cd UniData && python -m venv .venv && .venv/bin/pip install -r requirements-dev.txt
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
httpx>=0.25.0
# 运行期依赖（测试导入 app 包所需）
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
loguru>=0.7.2
argon2-cffi>=23.1.0
kafka-python>=2.0.2
