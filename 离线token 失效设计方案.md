# 离线 Token 失效设计方案

## 背景
当前系统使用离线 JWT 解析，天然无法做到“立刻失效”。为兼顾安全性与可用性，采用“本地缓存 + 分区 Redis + 广播同步 + 降级策略”的折中方案。

## 目标
- 撤销操作尽快生效（秒级）
- 搜索/写入链路高可用
- Redis 异常时有可控的退化行为

---

## 总体架构
1. **撤销中心**：写入主库（或中心 Redis）记录撤销信息
2. **广播同步**：撤销事件推送到消息队列（Kafka / MQ）
3. **区域缓存**：各区域订阅撤销事件并写入本地 Redis + 本地内存缓存
4. **请求校验**：JWT 验签通过后 → 先查本地内存缓存 → 再查本地 Redis

---

## 关键设计

### 1. 撤销记录（只保存被撤销的 jti）
- 字段：`jti`, `app_name`, `revoked_at`, `reason`
- 存储：主库（PostgreSQL）或中心 Redis
- 仅记录“已撤销”的 token，不保存所有 token 的 `jti`

### 2. 本地缓存结构
- 数据结构：`revoked:{jti} -> revoked_at`
- TTL：等于 Token 最大有效期（如 7 天）
- 内存缓存建议使用 LRU/TTL Cache，提高命中率，减轻 Redis 负载

### 3. 广播同步
- 产生撤销事件时，同时发送消息到 Kafka
- 各区域订阅并更新本地 Redis + 内存缓存

---

## 校验流程（请求进入时）
1. JWT 验签
2. 查本地内存缓存：
   - 命中 → 直接拒绝
3. 查本地 Redis：
   - 命中 → 拒绝，并写回内存缓存
4. 未命中 → 放行

说明：只要 `jti` 不在撤销列表中，就视为有效（离线签名已通过）。

---

## Redis 异常的折中策略（更稳妥）

### 读写分级
- **搜索接口（读）**：优先可用性 → Fail-Open
- **写入接口（写）**：优先安全 → Fail-Closed

### 具体策略
1. **Redis 短暂异常（< 30s）**
   - 仅依赖本地内存缓存
   - 放行未命中的请求
   - 触发告警

2. **Redis 长时间异常（>= 30s）**
   - 搜索接口继续 Fail-Open
   - 写入接口切换 Fail-Closed，返回 503/403

3. **恢复后**
   - 自动回到正常逻辑
   - 重刷最近撤销列表

---

## 兜底机制
- **启动时全量拉取**：同步最近 7 天撤销记录
- **定时增量拉取**：每 1-5 分钟补偿同步
- **监控告警**：Redis 不可用、同步落后、撤销堆积

---

## 优点与风险
### 优点
- 不依赖跨区 Redis，延迟低
- 异常时有明确退化策略
- 保障写入安全，搜索可用

### 风险
- 撤销传播有秒级延迟
- 本地缓存需控制内存大小

---

## 建议落地顺序
1. JWT 增加 `jti` 与 `iat`
2. 增加撤销表（仅保存撤销的 `jti`）+ 广播事件
3. 区域服务引入内存缓存 + Redis
4. 加入分级降级策略
5. 加监控与报警
